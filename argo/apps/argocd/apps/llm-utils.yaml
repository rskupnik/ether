apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: llm-utils
  namespace: argocd
spec:
  project: applications
  source:
    repoURL: https://github.com/rskupnik/ether.git
    targetRevision: HEAD
    path: argo/apps/llm-utils
  destination:
    server: https://kubernetes.default.svc
    namespace: llm
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
