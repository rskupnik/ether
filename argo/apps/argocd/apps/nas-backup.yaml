apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nas-backup
  namespace: argocd
spec:
  project: applications
  source:
    repoURL: https://github.com/rskupnik/ether.git
    targetRevision: HEAD
    path: argo/apps/nas-backup
  destination:
    server: https://kubernetes.default.svc
    namespace: nas-backup
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    # automated:
    #   prune: true
    #   selfHeal: true
