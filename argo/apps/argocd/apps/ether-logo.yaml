apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ether-logo
  namespace: argocd
spec:
  project: applications
  source:
    repoURL: https://github.com/rskupnik/ether.git
    targetRevision: HEAD
    path: argo/apps/ether-logo
  destination:
    server: https://kubernetes.default.svc
    namespace: ether-logo
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
