apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: upsnap
  namespace: argocd
spec:
  project: applications
  source:
    repoURL: https://github.com/rskupnik/ether.git
    targetRevision: HEAD
    path: argo/apps/upsnap
  destination:
    server: https://kubernetes.default.svc
    namespace: upsnap
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
