apiVersion: batch/v1
kind: CronJob
metadata:
  name: paperless-backup-orchestrator
  namespace: paperless
  labels:
    components.nora/ntfy-token: enabled
spec:
  schedule: "0 3 * * *"
  
  jobTemplate:
    spec:
      backoffLimit: 0
      template:
        spec:
          restartPolicy: Never
          serviceAccountName: paperless-backup-sa
          
          containers:
            - name: orchestrator
              image: bitnami/kubectl:latest
              command: ["/bin/sh", "-c"]
              args:
                - |
                  #!/usr/bin/env bash
                  set -Eeuo pipefail

                  notify_success() {
                    curl \
                      -H "Authorization: Bearer $NTFY_TOKEN" \
                      -H "Priority: min" \
                      -H "Tags: white_check_mark" \
                      -d "Paperless backup successful" \
                      --insecure \
                      https://ntfy.nora/backups
                  }

                  notify_failure() {
                    local exit_code="$1"
                    local failed_at="$2"

                    curl \
                      -H "Authorization: Bearer $NTFY_TOKEN" \
                      -H "Priority: high" \
                      -H "Tags: warning" \
                      -d "Paperless backup FAILED at: ${failed_at}. Exit code: ${exit_code}" \
                      --insecure \
                      https://ntfy.nora/backups || true
                  }

                  scale_paperless_up() {
                    echo "Scaling Paperless back up..."
                    kubectl scale deploy paperless --replicas=1 || true
                  }

                  on_error() {
                    local exit_code="$?"
                    local failed_command="${BASH_COMMAND}"

                    echo "Backup failed at command: $failed_command"
                    scale_paperless_up
                    notify_failure "$exit_code" "$failed_command"

                    exit "$exit_code"
                  }

                  trap on_error ERR

                  run_job() {
                    local job_name="$1"
                    local job_file="$2"
                    local timeout="${3:-2m}"

                    echo "Deleting old job: $job_name"
                    kubectl delete job "$job_name" --ignore-not-found --wait=true

                    echo "Applying job: $job_name"
                    kubectl apply -f "$job_file"

                    echo "Waiting for job to complete: $job_name"
                    kubectl wait "job/$job_name" --for=condition=complete --timeout="$timeout"

                    echo "Job completed: $job_name"
                  }

                  echo "Scaling Paperless down..."
                  kubectl scale deploy paperless --replicas=0

                  run_job "paperless-backup-media" "/configs/backup-media-job.yaml" "2m"
                  run_job "paperless-backup-db" "/configs/backup-db-job.yaml" "2m"
                  run_job "paperless-backup-redis" "/configs/backup-redis-job.yaml" "2m"

                  scale_paperless_up

                  notify_success

                  echo "Backup complete."
              envFrom: []
              volumeMounts:
                - name: job-definitions
                  mountPath: /configs
          
          volumes:
            - name: job-definitions
              configMap:
                name: paperless-backup-job-definitions
