namespace: seafile

helmCharts:
  - name: ce
    repo: https://haiwen.github.io/seafile-helm-chart/repo
    releaseName: seafile
    namespace: seafile
    version: "13.0"
    valuesFile: values.yaml

resources:
  - postgres/db-credentials-secret.sealed.yaml
  - postgres/statefulset.yaml
  - postgres/service.yaml