apiVersion: v1
kind: Service
metadata:
  name: mysql
  labels:
    app: mysql
spec:
  type: ClusterIP
  ports:
    - port: 33060
      targetPort: 33060
      protocol: TCP
      name: mysql
  selector:
    app: mysql
