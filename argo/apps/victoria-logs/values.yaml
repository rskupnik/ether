server:
  retentionPeriod: 7d

  persistentVolume:
    enabled: true
    size: 20Gi
    storageClassName: longhorn

  nodeSelector:
    kubernetes.io/hostname: ethernode-gmk

  resources:
    requests:
      cpu: 250m
      memory: 512Mi
    limits:
      cpu: "2"
      memory: 2Gi