apiVersion: v1
kind: Service
metadata:
  name: flaresolverr
  namespace: media
spec:
  type: ClusterIP

  selector:
    app.kubernetes.io/name: flaresolverr

  ports:
    - name: http
      port: 8191
      targetPort: http
      protocol: TCP