apiVersion: v1
kind: ServiceAccount
metadata:
  namespace: syncthing
  name: pvc-patch-sa
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
---
apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: syncthing
  name: pvc-patch-role
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
rules:
  - apiGroups: [""]
    resources: ["persistentvolumeclaims"]
    verbs: ["get", "list"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  namespace: syncthing
  name: pvc-patch-rolebinding
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
subjects:
  - kind: ServiceAccount
    name: pvc-patch-sa
roleRef:
  kind: Role
  name: pvc-patch-role
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  namespace: syncthing
  name: pv-patch-role
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
rules:
  - apiGroups: [""]
    resources: ["persistentvolumes"]
    verbs: ["get", "patch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  namespace: syncthing
  name: pv-patch-rolebinding
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
subjects:
  - kind: ServiceAccount
    name: pvc-patch-sa
roleRef:
  kind: ClusterRole
  name: pv-patch-role
  apiGroup: rbac.authorization.k8s.io
---
apiVersion: batch/v1
kind: Job
metadata:
  namespace: syncthing
  name: patch-pv-reclaim
  annotations:
    argocd.argoproj.io/sync-wave: "1"
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      restartPolicy: OnFailure
      serviceAccountName: pvc-patch-sa
      containers:
        - name: patch
          image: bitnamilegacy/kubectl:latest
          command:
            - /bin/sh
            - -c
            - |
              echo "[INFO] Waiting for PVC to bind..."
              until kubectl get pvc syncthing-data-pvc -n syncthing -o jsonpath='{.spec.volumeName}' | grep -q .; do
                sleep 2
              done

              PV=$(kubectl get pvc syncthing-data-pvc -n syncthing -o jsonpath='{.spec.volumeName}')
              echo "[INFO] Found PV: $PV"

              echo "[INFO] Patching reclaim policy to Retain..."
              kubectl patch pv "$PV" -p '{"spec":{"persistentVolumeReclaimPolicy":"Retain"}}'
              echo "[INFO] Patch complete."
